DECISIONS LOG — PASTE-IN TEXT FOR TODAY (9/10/2026)
====================================================
One SOP change to log: the per-prospect subfolder now gets created at
underwriting (when a prospect sends financials), not just at LOI.

--------------------------------------------------------------------
1) TOP OF FILE — replace the "Last updated" line
--------------------------------------------------------------------
Whatever the current "Last updated:" line reads at the top of the file,
prepend this to the front of the parenthetical (keep everything already
in there after it):

Per-prospect Dropbox subfolders now created at the underwriting stage (when a prospect sends financials), not only at LOI, closing a gap found on CL-3606 S 61st Ave Cir;

--------------------------------------------------------------------
2) "Decisions (settled)" section — insert ONE new entry
--------------------------------------------------------------------
Add this as a new top entry at the start of the Decisions (settled)
section (right after the "Decisions dated 8/20/2026–8/28/2026 have moved
to the archive file" note, before the most recent existing entry):

- **9/10/2026 — Commercial Listing Engagements SOP: per-prospect Dropbox subfolder now created at the underwriting stage, not only once an LOI is accepted — closes a real gap found on CL-3606 S 61st Ave Cir:** A prospective tenant sent financials for review on this listing and there was no defined folder to put them in — the SOP (Section 6, as of the 9/9/2026 split) only created a per-prospect subfolder once a specific prospect's LOI was accepted. Extended that same subfolder pattern one stage earlier: the moment a prospect sends anything for underwriting review (financials, a personal financial statement, a business plan), create `[Prospect Name] - [Property]/` under the listing root right then, with a `Financials/` subfolder inside it. If the deal later reaches LOI, that same folder just gains `LOI/`, `Lease/` or `PSA/`, `Closing/` per the existing Client Transaction Documents SOP structure — no second folder gets created. If the prospect doesn't move forward, the folder just stays as a record, same as an unclosed Stage 0 pitch.
  - **SOP updated:** `Client_Commercial_Listing_Engagements_-_SOP_(CL,_CS,_L).md` — Section 5 gained the per-prospect subfolder guidance (moved up from Section 6-only), Section 6 now checks for an existing subfolder before creating a new one, Section 8 got a new open item (whether "the moment they send anything for review" is too low a bar to trigger a folder, to revisit after a few more real deals), and Section 9's checklist got a new "prospect sends financials" entry.
  - **Applies to CL/CS/L/SL** via the same shared doc, same as the 9/9/2026 Leases/Financials split.

--------------------------------------------------------------------
That's it — 2 edits. This is on top of the 4 edits from yesterday
(9/9/2026) if those haven't gone in yet.
